import React from 'react';
import { Container } from 'react-bootstrap';

const Hours = () => {
  return (
    <Container id="hours">
      <h2>Hours</h2>
    </Container>
  );
};

export default Hours;
